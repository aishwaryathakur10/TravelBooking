@using Microsoft.AspNet.Identity
@using Microsoft.AspNet.Identity.Owin;

@if (Request.IsAuthenticated)
{
  

    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        
    @Html.AntiForgeryToken()

    <ul class="nav navbar-nav navbar-right">
        <li>
            @Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "Manage", "Account", routeValues: null, htmlAttributes: new { title = "Manage" })
        </li>

        

        <li>@Html.ActionLink("Bus Details", "Index", "Detail")</li>
        <li>@Html.ActionLink("Route Information", "Info", "RouteInfo")</li>

        @if(User.IsInRole("Admin"))
        {
             @*<li>@Html.ActionLink("Route-Management", "Index", "Admin")</li>*@ //This one of dropdown for source and destination.
           
            <li>@Html.ActionLink("City-Management", "Index", "City")</li>
          <li>@Html.ActionLink("Route-Day Management", "Index", "Day")</li>
            <li>@Html.ActionLink("Route creation ", "Index", "RouteInfo")</li>
            <li>@Html.ActionLink("Booking Info", "Index", "Booked")</li>
        }

      
             
                @*<li>@Html.ActionLink("Book Seat", "SeatInfo", "Booked")</li>*@




        <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
    </ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>
}
