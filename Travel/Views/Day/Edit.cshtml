@model Travel.Models.Table_day

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Table_day</h4>
        <hr />
        @Html.ValidationSummary(true)
        <div class="form-group">
            @Html.LabelFor(model => model.A_id, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.A_id)
                @Html.ValidationMessageFor(model => model.A_id)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.R_id, "R_id", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("R_id", String.Empty)
                @Html.ValidationMessageFor(model => model.R_id)
            </div>
        </div>

        @Html.HiddenFor(model => model.Day)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
